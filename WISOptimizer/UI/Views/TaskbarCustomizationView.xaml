<UserControl x:Class="WISOptimizer.UI.Views.TaskbarCustomizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource TextBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Taskbar &amp; UI Customization" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Streamline the Windows 11 taskbar for a cleaner operator experience." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="14"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
                <GroupBox Header="Taskbar Layout">
                    <StackPanel>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Taskbar icons left aligned" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkTaskbarLeft" IsChecked="{Binding TaskbarLeft}" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <Separator Background="{DynamicResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Hide Task View button" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkHideTaskView" IsChecked="{Binding HideTaskView}" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <Separator Background="{DynamicResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Remove Widgets/News panel" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkRemoveWidgets" IsChecked="{Binding RemoveWidgets}" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <Separator Background="{DynamicResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Hide Search box" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkHideSearch" IsChecked="{Binding HideSearch}" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Apply UI Settings" Style="{StaticResource ModernButton}" Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
