<UserControl x:Class="WISOptimizer.UI.Views.StartupServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock Text="Startup &amp; Services Cleanup" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Disable bloatware, unnecessary services and startup applications." Foreground="{StaticResource SecondaryTextBrush}" FontSize="14"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
                <GroupBox Header="Microsoft Bloatware">
                    <StackPanel>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Disable OneDrive" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkDisableOneDrive" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Disable Microsoft Teams" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkDisableTeams" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                            <Button Content="Uninstall OneDrive" Style="{StaticResource ModernButton}" Margin="0,0,8,0" Click="UninstallOneDrive_Click"/>
                            <Button Content="Uninstall Microsoft Teams" Style="{StaticResource ModernButton}" Click="UninstallTeams_Click"/>
                        </StackPanel>
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Disable Copilot" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkDisableCopilot" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Disable Weather Widget" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkDisableWeather" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Disable Xbox Services" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkDisableXbox" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <Separator Background="{StaticResource BorderBrush}" Margin="0,8"/>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Disable Bluetooth" VerticalAlignment="Center"/>
                            <CheckBox x:Name="chkDisableBluetooth" Style="{StaticResource ToggleSwitch}" DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                        </DockPanel>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Apply Cleanup" Style="{StaticResource ModernButton}" Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
